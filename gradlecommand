这里整理了一份全面的 Gradle 命令大全，涵盖了日常开发中最常用的命令和用法：

🔧 基础命令

项目信息命令

```bash
# 查看项目信息
gradle projects                    # 列出所有子项目
gradle tasks                       # 查看所有可用任务
gradle tasks --all                 # 查看所有任务（包括详细描述）
gradle dependencies                # 查看项目依赖
gradle dependencies --configuration <config> # 查看特定配置的依赖
```

构建命令

```bash
# 基本构建
gradle build                       # 执行完整构建（编译、测试、打包）
gradle clean                       # 清理构建输出
gradle clean build                 # 清理后重新构建
gradle assemble                    # 编译和打包，但不运行测试
gradle compileJava                 # 仅编译 Java 代码
gradle classes                     # 编译所有类文件
```

📦 测试相关命令

```bash
# 测试命令
gradle test                        # 运行所有测试
gradle test --tests "TestClass"    # 运行特定测试类
gradle test --tests "TestClass.method" # 运行特定测试方法
gradle test --tests "*Test"        # 运行所有以 Test 结尾的测试类
gradle test --continue             # 即使测试失败也继续执行
gradle test -i                     # 显示测试的详细信息

# 测试报告
gradle testReport                  # 生成测试报告
```

🔍 调试和排查命令

```bash
# 调试信息
gradle --version                   # 查看 Gradle 版本
gradle --status                    # 查看 Gradle Daemon 状态
gradle --stop                      # 停止所有 Gradle Daemon
gradle --help                      # 查看帮助信息

# 详细输出
gradle --info                      # 信息级别日志
gradle --debug                     # 调试级别日志
gradle --stacktrace                # 显示堆栈跟踪
gradle --full-stacktrace           # 显示完整堆栈跟踪
gradle --scan                      # 生成构建扫描报告
```

🎯 任务执行命令

```bash
# 任务执行
gradle <taskName>                  # 执行特定任务
gradle :subproject:task            # 执行子项目的任务
gradle task1 task2                 # 执行多个任务
gradle -q <task>                   # 安静模式（只输出错误）
gradle -x test build               # 排除特定任务（不运行测试）

# 任务信息
gradle help --task <taskName>      # 查看特定任务的帮助
gradle properties                  # 查看项目属性
```

📚 依赖管理命令

```bash
# 依赖管理
gradle dependencies                # 查看依赖树
gradle dependencyInsight --dependency <name> --configuration <config>
gradle androidDependencies         # Android 项目查看依赖
gradle dependencies --scan         # 生成依赖扫描报告

# 依赖更新
gradle dependencyUpdates           # 检查依赖更新（需要插件）
```

🚀 Android 项目专用命令

```bash
# Android 构建
gradle assembleDebug               # 构建 Debug APK
gradle assembleRelease             # 构建 Release APK
gradle installDebug                # 安装 Debug 版本
gradle uninstallDebug              # 卸载 Debug 版本

# Android 测试
gradle connectedAndroidTest        # 在连接的设备上运行测试
gradle lint                        # 运行代码检查
```

⚡ 性能优化命令

```bash
# 性能相关
gradle --profile                   # 生成性能分析报告
gradle --dry-run                   # 模拟运行（不实际执行任务）
gradle --no-daemon                 # 不使用 Daemon
gradle --parallel                  # 并行执行任务
gradle --build-cache               # 启用构建缓存
gradle --configure-on-demand       # 按需配置
```

🔄 包装器命令

```bash
# Gradle Wrapper
gradle wrapper                     # 生成 wrapper 文件
gradle wrapper --gradle-version 8.5 # 指定 Gradle 版本
./gradlew <command>                # 使用 wrapper 执行命令（推荐）
```

🛠️ 实用技巧

1. 条件执行

```bash
# 只在有变化时执行
gradle build --rerun-tasks

# 离线模式（使用缓存）
gradle build --offline
```

2. 属性配置

```bash
# 设置属性
gradle build -PmyProperty=value    # 项目属性
gradle build -DsystemProp=value    # 系统属性
gradle build -x test               # 排除任务
```

3. 构建扫描

```bash
# 生成详细的构建分析报告
gradle build --scan
```

📝 常用命令组合

```bash
# 开发常用组合
./gradlew clean build              # 清理并重新构建
./gradlew clean test               # 清理并运行测试
./gradlew assembleDebug --parallel # 并行构建 Debug 版本
./gradlew build -x test            # 构建但不运行测试

# CI/CD 常用
./gradlew clean build --scan       # 清理构建并生成扫描报告
./gradlew test --continue          # 运行测试（失败也继续）
```

💡 使用建议

1. 优先使用 ./gradlew（Wrapper）而不是全局 gradle 命令
2. 使用 Daemon 提高构建速度（默认启用）
3. 合理使用缓存 和并行执行
4. 定期使用 --scan 分析构建性能
5. 使用 --continue 在 CI 环境中即使部分失败也继续执行

这份命令大全涵盖了 Gradle 的主要使用场景，建议根据实际需求选择合适的命令组合。

