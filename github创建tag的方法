下面是一份快速、准确的 GitHub Tag 创建方法总结：你可以用命令行创建，也可以直接在 GitHub 网页上创建。两种方式我都整理成最清晰的步骤。

---

🏷️ GitHub Tag 的创建方法（命令行 & 网页）

GitHub 的 Tag 本质上是给某个 commit 打一个“版本标签”，常用于发布版本（如 v1.0.0）。
以下内容基于搜索结果整理 ビズドットオンライン +1。

---

✅ 一、使用命令行创建 Tag（最常用）

1. 创建轻量标签（lightweight tag）

git tag v1.0.0


• 直接指向当前最新 commit
• 不包含作者、日期、注释等元数据 Qiita


---

2. 创建附注标签（annotated tag）— 推荐

git tag -a v1.0.0 -m "Release version 1.0.0"


• 包含作者、日期、注释
• 更适合正式发布版本 Qiita


---

3. 给过去的某个 commit 打 tag

git tag -a v1.0.0 -m "Release 1.0.0" <commit-hash>


例如：

git tag -a v1.0.0 -m "Release 1.0.0" 9fceb02


适用于“忘记打 tag”的情况 Qiita。

---

4. 推送 tag 到 GitHub

创建 tag 后不会自动上传，需要手动 push：

推送单个 tag

git push origin v1.0.0


推送所有本地 tag

git push origin --tags


Qiita

---

✅ 二、在 GitHub 网页上创建 Tag（无需命令行）

GitHub 也支持直接在网页上创建 Tag DEV Community：

1. 打开你的仓库
2. 点击 Releases
3. 点击 Draft a new release
4. 在 “Tag version” 输入框中输入新 tag 名称（如 v1.0.0）
5. 选择要关联的 commit（默认是 main 最新 commit）
6. 点击 Publish release


这会自动创建一个 tag 并发布一个 Release。

---

🧹 三、查看 Tag

git tag


按字母顺序列出所有 tag Qiita。

查看某个 tag 的详细信息：

git show v1.0.0


Qiita

---

🗑️ 四、删除 Tag（可选）

删除本地 tag

git tag -d v1.0.0


删除远程 tag

git push origin :refs/tags/v1.0.0


---

如果你愿意，我可以帮你写一个 适合你项目的 Tag 命名规范（如语义化版本 SemVer），或者帮你检查你当前仓库的 Tag 使用是否合理。


