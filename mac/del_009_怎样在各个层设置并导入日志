在Spring Boot项目的不同层级中，配置Logger的方式略有不同。以下是各层的具体配置方法：

---

Controller层 - 使用@Slf4j注解‌

kotlin

@RestController
@RequestMapping("/api/users")
@Slf4j
class UserController {
    
    @GetMapping
    fun getAllUsers(): List<User> {
        log.info("收到获取用户列表请求")
        // 业务逻辑
    }
}


---


Service层 - 使用LoggerFactory‌

kotlin

@Service
class UserService {
    
    companion object {
        private val log = LoggerFactory.getLogger(UserService::class.java)
    }

    fun createUser(user: User): User {
        log.debug("开始创建用户: ${user.username}")
        // 业务逻辑
        log.info("用户创建成功，ID: ${user.id}")
    }
}


---


Mapper层 - 同样使用LoggerFactory
‌

kotlin

@Mapper
interface UserMapper {
    
    companion object {
        private val log = LoggerFactory.getLogger(UserMapper::class.java)
    }

    fun findById(id: Long): User? {
        log.trace("执行SQL查询，用户ID: $id")
        // 数据库操作
    }
}


---


Entity/Model层 - 通常不直接使用Logger


实体类主要职责是数据承载，不处理业务逻辑
如需日志记录，应在Service层进行
配置要点：‌

使用SLF4J作为日志门面，Logback作为默认实现


---


在application.yml中配置日志级别：
yaml

logging:
  level:
    com.example.demo.controller: DEBUG
    com.example.demo.service: INFO
    com.example.demo.mapper: TRACE


---


💡 实用建议‌：
在 Service层 使用 DEBUG 级别记录详细的业务处理过程，
在 Controller层 使用 INFO 级别记录请求响应，
这样既能满足调试需求又不会产生过多日志。


---



