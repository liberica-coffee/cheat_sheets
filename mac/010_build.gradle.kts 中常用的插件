build.gradle.kts 中常用的插件主要分为以下几类：

核心构建插件‌

Kotlin插件‌：
  kotlin("jvm")、
  kotlin("plugin.spring")、
  kotlin("plugin.jpa")

Spring Boot插件‌：
  id("org.springframework.boot")

依赖管理插件‌：
  id("io.spring.dependency-management")

开发效率插件‌

Kapt插件‌：
  kotlin("kapt")，用于Kotlin注解处理

数据库相关插件‌
  MyBatis插件‌：id("org.mybatis.spring.boot")，用于集成MyBatis框架


💡 实用建议‌：建议使用 plugins 块来声明插件，这种方式语法简洁且支持自动下载。对于第三方插件记得指定版本号，而官方核心插件则无需版本。

